# Reconcile AI — Free Open Source Demo

**Compare two CSV files in seconds** — find mismatches, anomalies, and fuzzy matches.

Live demo: [https://your-app.streamlit.app](https://your-app.streamlit.app)

### Features
- Auto key detection (e.g., OrderID + ClientCode)
- Fuzzy string matching
- Numeric tolerance
- Anomaly detection with AI
- Sample data included

### Run Locally
```bash
pip install -r requirements.txt
streamlit run reconciler_demo.py
